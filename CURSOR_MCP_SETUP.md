# 🚀 Быстрая настройка GitHub MCP в Cursor

## ✅ Шаг 1: Файл конфигурации создан

Файл конфигурации уже создан по адресу:
```
C:\Users\YOUR_USER\AppData\Roaming\Cursor\User\globalStorage\rooveterinaryinc.roo-cline\mcp.json
```

## 🔑 Шаг 2: Создайте GitHub Personal Access Token

1. Перейдите на: **https://github.com/settings/tokens**
2. Нажмите **"Generate new token (classic)"**
3. Название: `Cursor MCP`
4. Выберите разрешения:
   - ✅ `repo` (полный доступ)
   - ✅ `read:org` (чтение организаций)
   - ✅ `read:user` (информация о пользователе)
5. Нажмите **"Generate token"**
6. **Скопируйте токен** (показывается только один раз!)

## 📝 Шаг 3: Добавьте токен в конфигурацию

### Способ 1: Через PowerShell

```powershell
# Откройте файл в блокноте
notepad "$env:APPDATA\Cursor\User\globalStorage\rooveterinaryinc.roo-cline\mcp.json"
```

Замените `YOUR_GITHUB_TOKEN_HERE` на ваш токен.

### Способ 2: Через Cursor

1. Откройте Cursor
2. Нажмите `Ctrl+Shift+P`
3. Введите: `Preferences: Open User Settings (JSON)`
4. Или найдите файл конфигурации вручную

## 🔄 Шаг 4: Перезапустите Cursor

**ВАЖНО:** Полностью закройте Cursor (не просто окно, а весь процесс) и откройте заново.

## ✅ Шаг 5: Проверка

После перезапуска Cursor:
1. GitHub MCP должен появиться в списке MCP серверов
2. Попробуйте спросить ассистента: "Используй GitHub MCP для..."

## 📋 Текущая конфигурация

Файл `mcp.json` содержит:

```json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-github"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "YOUR_GITHUB_TOKEN_HERE"
      }
    }
  }
}
```

## ⚠️ Важно

- **НЕ** коммитьте файл `mcp.json` с токеном в Git
- Храните токен только локально
- Используйте минимально необходимые разрешения

## 🐛 Если не работает

1. Проверьте, что токен правильный (без пробелов в начале/конце)
2. Убедитесь, что Node.js установлен: `node --version`
3. Проверьте, что npm доступен: `npm --version`
4. Попробуйте перезапустить Cursor еще раз
5. Проверьте логи Cursor на наличие ошибок



