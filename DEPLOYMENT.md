# 🚀 Руководство по деплою VEOX

## Быстрый старт

### 1. Подготовка к деплою

Убедитесь, что у вас есть:
- ✅ Аккаунт на Supabase
- ✅ Аккаунт на Stripe
- ✅ Аккаунт на Vercel (или другой платформе)
- ✅ (Опционально) Аккаунт на Resend для email

### 2. Настройка Supabase

1. Создайте проект на [supabase.com](https://supabase.com)
2. Выполните SQL из `supabase/schema.sql` в SQL Editor
3. Скопируйте:
   - Project URL → `NEXT_PUBLIC_SUPABASE_URL`
   - anon key → `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### 3. Настройка Stripe

1. Создайте аккаунт на [stripe.com](https://stripe.com)
2. Получите ключи из Dashboard → Developers → API keys
3. Скопируйте:
   - Publishable key → `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`
   - Secret key → `STRIPE_SECRET_KEY`

### 4. Деплой на Vercel

#### Вариант A: Через GitHub

1. Загрузите код в GitHub репозиторий
2. Перейдите на [vercel.com](https://vercel.com)
3. Нажмите "New Project"
4. Импортируйте репозиторий
5. Добавьте переменные окружения:

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key
STRIPE_SECRET_KEY=your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=your_webhook_secret
NEXT_PUBLIC_APP_URL=https://your-domain.vercel.app
RESEND_API_KEY=your_resend_key (опционально)
EMAIL_FROM=VEOX <noreply@veox.ee>
```

6. Нажмите "Deploy"

#### Вариант B: Через Vercel CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Логин
vercel login

# Деплой
vercel

# Production деплой
vercel --prod
```

### 5. Настройка Stripe Webhook

После деплоя:

1. Перейдите в Stripe Dashboard → Developers → Webhooks
2. Нажмите "Add endpoint"
3. URL: `https://your-domain.vercel.app/api/stripe/webhook`
4. События: `checkout.session.completed`
5. Скопируйте Signing secret → `STRIPE_WEBHOOK_SECRET`
6. Обновите переменную в Vercel

### 6. Настройка Supabase Redirect URLs

1. Supabase Dashboard → Authentication → URL Configuration
2. Добавьте:
   - `https://your-domain.vercel.app/ru/auth/callback`
   - `https://your-domain.vercel.app/et/auth/callback`

### 7. Настройка Google OAuth (опционально)

1. Google Cloud Console → Create OAuth 2.0 Client
2. Authorized redirect URIs:
   - `https://your-project.supabase.co/auth/v1/callback`
3. Скопируйте Client ID и Secret в Supabase

---

## Деплой на другие платформы

### Netlify

1. Подключите репозиторий
2. Build command: `npm run build`
3. Publish directory: `.next`
4. Добавьте переменные окружения

### Railway

1. Создайте новый проект
2. Подключите GitHub репозиторий
3. Добавьте переменные окружения
4. Railway автоматически определит Next.js

### AWS Amplify

1. Подключите репозиторий
2. Build settings:
   - Build command: `npm run build`
   - Output directory: `.next`
3. Добавьте переменные окружения

---

## Проверка после деплоя

### ✅ Чеклист

- [ ] Сайт открывается
- [ ] Регистрация работает
- [ ] Авторизация работает
- [ ] Создание заказа работает
- [ ] Платежи Stripe работают
- [ ] Webhook получает события
- [ ] Email отправляется (если настроен)
- [ ] Переключение языков работает
- [ ] RLS политики работают

### Тестирование платежей

Используйте тестовые карты Stripe:
- Успешный платеж: `4242 4242 4242 4242`
- Отклоненный: `4000 0000 0000 0002`

---

## Мониторинг

### Vercel Analytics

Включите Analytics в Vercel Dashboard для отслеживания:
- Производительность
- Ошибки
- Трафик

### Supabase Dashboard

Мониторьте:
- Использование БД
- Auth события
- Edge Function логи

### Stripe Dashboard

Отслеживайте:
- Платежи
- Webhook события
- Ошибки

---

## Обновление

### Обновление кода

```bash
# Локально
git pull
npm install
npm run build

# Vercel автоматически обновит при push в main
git push origin main
```

### Обновление БД

1. Создайте миграцию в Supabase
2. Выполните SQL в SQL Editor
3. Проверьте RLS политики

---

## Troubleshooting

### Ошибка: "Supabase URL not found"
- Проверьте переменные окружения
- Убедитесь, что `NEXT_PUBLIC_` префикс есть

### Ошибка: "Stripe webhook failed"
- Проверьте `STRIPE_WEBHOOK_SECRET`
- Убедитесь, что endpoint правильный
- Проверьте логи в Stripe Dashboard

### Ошибка: "RLS policy violation"
- Проверьте RLS политики в Supabase
- Убедитесь, что пользователь авторизован
- Проверьте права доступа

### Email не отправляется
- Проверьте `RESEND_API_KEY`
- Убедитесь, что домен настроен
- Проверьте логи в Resend Dashboard

---

## Бэкапы

### Supabase

Supabase автоматически создает бэкапы:
- Ежедневные бэкапы (Pro план)
- Ручной экспорт через Dashboard

### Рекомендации

1. Регулярно экспортируйте схему БД
2. Храните переменные окружения в безопасном месте
3. Документируйте изменения

---

## Масштабирование

### Когда переходить на платные тарифы

**Supabase:**
- > 500MB БД
- > 2GB bandwidth
- Нужны более частые бэкапы

**Stripe:**
- Production режим (вместо test)
- Нужны реальные платежи

**Resend:**
- > 3000 писем/месяц
- Нужна лучшая доставляемость

---

## Безопасность

### Рекомендации

1. ✅ Используйте HTTPS (автоматически на Vercel)
2. ✅ Храните секреты в переменных окружения
3. ✅ Не коммитьте `.env.local`
4. ✅ Регулярно обновляйте зависимости
5. ✅ Мониторьте логи на ошибки
6. ✅ Используйте RLS для защиты данных

---

**Готово! Ваш проект развернут и готов к использованию! 🎉**




