# Установка MCP Server для GitHub

## ✅ Пакет установлен

Пакет `@modelcontextprotocol/server-github` установлен глобально.

⚠️ **Примечание:** Пакет помечен как deprecated, но все еще должен работать. Возможно, в будущем будет новая версия.

## 📝 Настройка в Cursor IDE

### Шаг 1: Создайте GitHub Personal Access Token

1. Перейдите на https://github.com/settings/tokens
2. Нажмите "Generate new token (classic)"
3. Название токена: `Cursor MCP`
4. Выберите разрешения:
   - ✅ `repo` (полный доступ к репозиториям)
   - ✅ `read:org` (чтение организаций)
   - ✅ `read:user` (чтение информации о пользователе)
5. Нажмите "Generate token"
6. **Скопируйте токен** (он показывается только один раз!)

### Шаг 2: Найдите конфигурационный файл Cursor

**Windows:**
```
%APPDATA%\Cursor\User\globalStorage\rooveterinaryinc.roo-cline\mcp.json
```

Или через PowerShell:
```powershell
$env:APPDATA + "\Cursor\User\globalStorage\rooveterinaryinc.roo-cline\mcp.json"
```

**macOS:**
```
~/Library/Application Support/Cursor/User/globalStorage/rooveterinaryinc.roo-cline/mcp.json
```

**Linux:**
```
~/.config/Cursor/User/globalStorage/rooveterinaryinc.roo-cline/mcp.json
```

### Шаг 3: Создайте или обновите файл конфигурации

Создайте файл `mcp.json` в указанной директории (если его нет) или откройте существующий.

Добавьте следующую конфигурацию:

```json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-github"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "ВСТАВЬТЕ_ВАШ_ТОКЕН_СЮДА"
      }
    }
  }
}
```

⚠️ **Замените** `ВСТАВЬТЕ_ВАШ_ТОКЕН_СЮДА` на ваш GitHub Personal Access Token.

### Шаг 4: Перезапустите Cursor

Полностью закройте и откройте Cursor IDE заново.

## ✅ Проверка работы

После перезапуска Cursor, MCP server должен быть доступен. Вы можете проверить это, попросив ассистента использовать GitHub функции.

## 🔒 Безопасность

- ⚠️ **НЕ коммитьте** файл `mcp.json` с токеном в Git
- ⚠️ Токен должен храниться только локально
- ⚠️ Используйте минимально необходимые разрешения для токена
- ⚠️ Регулярно обновляйте токены

## 📋 Альтернативный способ (если глобальная установка не работает)

Если глобальная установка не работает, можно использовать локальную установку в проекте:

```bash
npm install @modelcontextprotocol/server-github --save-dev
```

И обновить конфигурацию:
```json
{
  "mcpServers": {
    "github": {
      "command": "node",
      "args": [
        "./node_modules/@modelcontextprotocol/server-github/dist/index.js"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "ваш_токен"
      }
    }
  }
}
```

## 🐛 Troubleshooting

### MCP server не подключается
- Проверьте, что путь к конфигурационному файлу правильный
- Убедитесь, что токен правильный и не истек
- Проверьте права доступа токена
- Посмотрите логи Cursor на наличие ошибок

### Токен не работает
- Убедитесь, что токен скопирован полностью (без пробелов)
- Проверьте, что токен имеет необходимые разрешения
- Создайте новый токен, если старый не работает

### npx не найден
- Убедитесь, что Node.js установлен: `node --version`
- Проверьте, что npm доступен: `npm --version`
- Переустановите Node.js, если необходимо

## 📚 Дополнительные ресурсы

- [MCP Documentation](https://modelcontextprotocol.io/)
- [GitHub Personal Access Tokens](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/managing-your-personal-access-tokens)
- [Cursor IDE Documentation](https://cursor.sh/docs)




