# 🚀 Быстрый старт VEOX

## Шаг 1: Создайте файл .env.local

Создайте файл `.env.local` в корне проекта со следующим содержимым:

```env
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Stripe Configuration (можно оставить пустым для начала)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Email Configuration (опционально)
RESEND_API_KEY=re_...
EMAIL_FROM=VEOX <noreply@veox.ee>
```

## Шаг 2: Получите ключи Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Создайте новый проект (или используйте существующий)
3. Перейдите в Settings → API
4. Скопируйте:
   - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public key** → `NEXT_PUBLIC_SUPABASE_ANON_KEY`

## Шаг 3: Настройте базу данных

1. В Supabase Dashboard откройте SQL Editor
2. Скопируйте содержимое файла `supabase/schema.sql`
3. Вставьте и выполните SQL скрипт

## Шаг 4: Запустите сервер

```bash
npm run dev
```

Откройте http://localhost:3000

## ⚠️ Важно

- Без настроенных переменных Supabase приложение будет работать, но функции аутентификации не будут доступны
- Для полной функциональности настройте также Stripe (см. `SETUP.md`)
- Email уведомления опциональны, но рекомендуются

## 📚 Подробная настройка

См. `SETUP.md` для детальных инструкций по настройке всех компонентов.




