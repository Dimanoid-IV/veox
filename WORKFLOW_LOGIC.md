# Логика взаимодействия заказчика и исполнителя

## 🔄 Полный цикл работы

### 1. Создание заказа

**Заказчик:**
- Создает заказ через `/create-order`
- Заказ сохраняется со статусом `open`
- Заказ виден всем исполнителям в `/performer/orders`

**Исполнитель:**
- Видит новые заказы в списке `/performer/orders`
- Может откликнуться на заказ со статусом `open`

---

### 2. Отклик исполнителя

**Исполнитель:**
- Нажимает "Откликнуться" на странице заказа
- Заполняет форму: цена (опционально) и сообщение
- Отклик создается со статусом `pending`

**Заказчик:**
- Получает уведомление (in-app и email)
- Видит отклик на странице своего заказа
- Видит имя исполнителя, рейтинг, цену и сообщение

**Логика:**
- Исполнитель может откликнуться только на заказы со статусом `open`
- Исполнитель может откликнуться только один раз на каждый заказ
- После отклика кнопка "Откликнуться" заменяется на "Ожидание ответа заказчика"

---

### 3. Принятие предложения заказчиком

**Заказчик:**
- На странице заказа видит список всех откликов
- Нажимает "Принять предложение" на выбранном отклике
- Система автоматически:
  - Меняет статус выбранного offer на `accepted`
  - Отклоняет все остальные offer (статус `rejected`)
  - Меняет статус заказа на `in_progress`

**Исполнитель (чей offer принят):**
- Получает уведомление (in-app и email)
- Теперь видит кнопку "Получить контакты"

**Логика:**
- Принять предложение может только владелец заказа
- Принять можно только предложения со статусом `pending`
- После принятия заказ переходит в статус `in_progress`
- Другие исполнители не могут больше откликаться

---

### 4. Покупка контактов

**Исполнитель:**
- После принятия предложения видит кнопку "Получить контакты"
- Нажимает кнопку → переход на страницу `/performer/orders/[id]/purchase`
- Оплачивает 5€ через Stripe Checkout
- После успешной оплаты получает доступ к контактам

**Заказчик:**
- Получает уведомление, что его контакты были куплены

**Логика:**
- Покупить контакты может только исполнитель с принятым offer (`status = 'accepted'`)
- Контакты доступны только после оплаты
- Один исполнитель может купить контакты только один раз для каждого заказа

---

### 5. Доступ к контактам

**Исполнитель:**
- После оплаты видит контакты на странице заказа:
  - Имя заказчика
  - Телефон
  - Email
  - Ссылка на WhatsApp

**Заказчик:**
- Всегда видит свои контакты
- Может видеть, кто купил доступ к контактам

**Логика:**
- Контакты скрыты до покупки (показывается только "Контакты заказчика")
- После покупки контакты доступны только купившему исполнителю
- Другие исполнители не видят контакты

---

### 6. Выполнение заказа

**Исполнитель:**
- Связывается с заказчиком по контактам
- Выполняет работу
- После выполнения заказчик может оставить отзыв

**Заказчик:**
- Связывается с исполнителем
- После выполнения заказа может оставить отзыв и рейтинг

**Логика:**
- Заказ остается в статусе `in_progress` до завершения
- После отзыва заказ переходит в статус `completed`

---

## 📊 Статусы заказов

- `open` - заказ открыт, исполнители могут откликаться
- `in_progress` - предложение принято, работа выполняется
- `completed` - работа выполнена, отзыв оставлен
- `cancelled` - заказ отменен

## 📊 Статусы предложений (offers)

- `pending` - предложение отправлено, ожидает ответа заказчика
- `accepted` - предложение принято заказчиком
- `rejected` - предложение отклонено (автоматически при принятии другого)
- `completed` - заказ выполнен

---

## 🔒 Правила доступа

### Заказчик видит:
- ✅ Все свои заказы (все статусы)
- ✅ Все отклики на свои заказы
- ✅ Свои контакты всегда
- ✅ Может принимать отклики только на свои заказы

### Исполнитель видит:
- ✅ Все открытые заказы (`open`)
- ✅ Заказы, на которые откликнулся (`in_progress` с его offer)
- ✅ Может откликаться только на `open` заказы
- ✅ Может купить контакты только если его offer принят
- ✅ Видит контакты только после оплаты

---

## ✅ Исправленная логика

1. ✅ Исполнитель может откликнуться только на `open` заказы
2. ✅ Исполнитель может откликнуться только один раз
3. ✅ Заказчик может принять только одно предложение
4. ✅ После принятия все остальные предложения отклоняются
5. ✅ Статус заказа меняется на `in_progress` после принятия
6. ✅ Кнопка "Получить контакты" показывается только если offer принят
7. ✅ Контакты доступны только после оплаты
8. ✅ Правильные уведомления для всех этапов
9. ✅ Защита на уровне API от несанкционированного доступа

---

## 🎯 API Endpoints

### POST `/api/offers/create`
- Создает новый отклик исполнителя
- Проверяет права исполнителя
- Проверяет статус заказа
- Создает уведомление заказчику

### POST `/api/offers/accept`
- Принимает предложение исполнителя
- Отклоняет остальные предложения
- Обновляет статус заказа
- Создает уведомление исполнителю

### POST `/api/stripe/create-payment-intent`
- Создает платежную сессию Stripe
- Проверяет, что offer принят
- Проверяет, что контакты еще не куплены
- Создает запись `contact_purchase` со статусом `pending`

### POST `/api/stripe/webhook`
- Обрабатывает успешную оплату
- Обновляет статус `contact_purchase` на `completed`
- Создает уведомление заказчику

---

## 📝 Чеклист проверки

- [ ] Заказчик создает заказ → статус `open`
- [ ] Исполнитель откликается → создается offer со статусом `pending`
- [ ] Заказчик получает уведомление
- [ ] Заказчик видит отклик на странице заказа
- [ ] Заказчик принимает отклик → offer `accepted`, остальные `rejected`, заказ `in_progress`
- [ ] Исполнитель получает уведомление о принятии
- [ ] Исполнитель видит кнопку "Получить контакты"
- [ ] Исполнитель оплачивает → получает доступ к контактам
- [ ] Контакты видны только купившему исполнителю
- [ ] Другие исполнители не видят контакты

---

## 🐛 Известные ограничения

1. Заказчик может принять только одно предложение (по дизайну)
2. После принятия другие исполнители не могут откликаться (заказ в `in_progress`)
3. Контакты покупаются один раз на заказ (UNIQUE constraint в БД)
4. Отзыв можно оставить только после выполнения заказа




