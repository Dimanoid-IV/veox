# 🔧 Подключение Supabase к VEOX

## Шаг 1: Создание проекта в Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Нажмите **"Start your project"** или **"Sign in"**
3. Войдите через GitHub, Google или создайте аккаунт
4. Нажмите **"New Project"**
5. Заполните форму:
   - **Name**: `veox` (или любое другое имя)
   - **Database Password**: придумайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший регион (например, `West EU` для Эстонии)
   - **Pricing Plan**: выберите **Free** (бесплатный план)
6. Нажмите **"Create new project"**
7. Подождите 1-2 минуты, пока проект создается

---

## Шаг 2: Получение API ключей

1. В Dashboard вашего проекта перейдите в **Settings** (шестеренка внизу слева)
2. Выберите **API** в меню слева
3. Найдите секцию **Project API keys**

Вам понадобятся два значения:

### 1. Project URL
- Находится в секции **Project URL**
- Пример: `https://abcdefghijklmnop.supabase.co`
- Скопируйте это значение

### 2. anon public key
- Находится в секции **Project API keys** → **anon public**
- Это длинная строка, начинающаяся с `eyJ...`
- Скопируйте это значение

⚠️ **Важно**: Используйте именно **anon public** ключ, а не **service_role**!

---

## Шаг 3: Создание файла .env.local

1. В корне проекта VEOX создайте файл `.env.local`
2. Откройте его в редакторе
3. Добавьте следующие строки:

```env
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-ключ-здесь
```

**Пример:**
```env
NEXT_PUBLIC_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTYzODk2NzI5MCwiZXhwIjoxOTU0NTQzMjkwfQ.ваш-ключ-продолжается-здесь
```

---

## Шаг 4: Настройка базы данных

1. В Supabase Dashboard перейдите в **SQL Editor** (иконка базы данных в меню слева)
2. Нажмите **"New query"**
3. Откройте файл `supabase/schema.sql` из проекта VEOX
4. Скопируйте **весь** содержимое файла
5. Вставьте в SQL Editor в Supabase
6. Нажмите **"Run"** (или `Ctrl+Enter`)
7. Дождитесь сообщения **"Success. No rows returned"**

✅ Если все прошло успешно, вы увидите сообщение об успешном выполнении.

---

## Шаг 5: Настройка аутентификации

### Настройка Email аутентификации

1. В Dashboard перейдите в **Authentication** → **Providers**
2. Убедитесь, что **Email** включен (по умолчанию включен)
3. В разделе **Email Auth** можно настроить:
   - **Enable email confirmations** - включите для production
   - **Site URL** - укажите `http://localhost:3000` для разработки

### Настройка Google OAuth (опционально)

1. В **Authentication** → **Providers** найдите **Google**
2. Нажмите **"Enable Google provider"**
3. Вам понадобится:
   - **Client ID** из Google Cloud Console
   - **Client Secret** из Google Cloud Console

#### Как получить Google OAuth credentials:

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com)
2. Создайте новый проект или выберите существующий
3. Перейдите в **APIs & Services** → **Credentials**
4. Нажмите **"Create Credentials"** → **"OAuth client ID"**
5. Выберите **"Web application"**
6. Добавьте **Authorized redirect URIs**:
   ```
   https://ваш-проект-id.supabase.co/auth/v1/callback
   ```
7. Скопируйте **Client ID** и **Client Secret**
8. Вставьте их в Supabase Dashboard

---

## Шаг 6: Настройка Redirect URLs

1. В Dashboard перейдите в **Authentication** → **URL Configuration**
2. В разделе **Redirect URLs** добавьте:
   ```
   http://localhost:3000/ru/auth/callback
   http://localhost:3000/et/auth/callback
   ```
3. Для production добавьте также:
   ```
   https://your-domain.com/ru/auth/callback
   https://your-domain.com/et/auth/callback
   ```

---

## Шаг 7: Проверка подключения

1. Перезапустите dev сервер:
   ```bash
   # Остановите сервер (Ctrl+C)
   npm run dev
   ```

2. Откройте http://localhost:3000

3. Попробуйте зарегистрироваться:
   - Перейдите на `/ru/signup`
   - Заполните форму
   - Проверьте email для подтверждения

4. Проверьте базу данных:
   - В Supabase Dashboard → **Table Editor**
   - Должна появиться таблица `profiles` с вашим пользователем

---

## Шаг 8: Настройка Storage (опционально, для загрузки изображений)

1. В Dashboard перейдите в **Storage**
2. Нажмите **"Create a new bucket"**
3. Название: `order-images`
4. **Public bucket**: включите (чтобы изображения были доступны публично)
5. Нажмите **"Create bucket"**

### Настройка RLS для Storage

1. В **Storage** → **Policies** выберите bucket `order-images`
2. Создайте политику:
   - **Policy name**: `Allow public read`
   - **Allowed operation**: `SELECT`
   - **Policy definition**: 
     ```sql
     true
     ```
   - **Policy command**: `SELECT`

3. Создайте еще одну политику:
   - **Policy name**: `Allow authenticated upload`
   - **Allowed operation**: `INSERT`
   - **Policy definition**:
     ```sql
     auth.role() = 'authenticated'
     ```
   - **Policy command**: `INSERT`

---

## ✅ Чеклист проверки

- [ ] Проект создан в Supabase
- [ ] API ключи скопированы в `.env.local`
- [ ] SQL схема выполнена успешно
- [ ] Redirect URLs настроены
- [ ] Email аутентификация работает
- [ ] (Опционально) Google OAuth настроен
- [ ] (Опционально) Storage настроен

---

## 🔍 Проверка работы

### Проверка через Supabase Dashboard

1. **Table Editor**: должны быть видны таблицы:
   - `profiles`
   - `orders`
   - `offers`
   - `contact_purchases`
   - `reviews`
   - `performer_profiles`
   - `notifications`

2. **Authentication** → **Users**: после регистрации должен появиться пользователь

3. **SQL Editor**: выполните тестовый запрос:
   ```sql
   SELECT * FROM profiles LIMIT 5;
   ```

### Проверка через приложение

1. Откройте http://localhost:3000/ru
2. Нажмите **"Войти"**
3. Попробуйте зарегистрироваться
4. После регистрации проверьте, что вы можете войти

---

## 🐛 Решение проблем

### Ошибка: "Missing Supabase environment variables"

**Решение:**
1. Убедитесь, что файл `.env.local` создан в корне проекта
2. Проверьте, что переменные названы правильно:
   - `NEXT_PUBLIC_SUPABASE_URL` (с префиксом `NEXT_PUBLIC_`)
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` (с префиксом `NEXT_PUBLIC_`)
3. Перезапустите dev сервер после создания/изменения `.env.local`

### Ошибка: "Invalid API key"

**Решение:**
1. Убедитесь, что используете **anon public** ключ, а не service_role
2. Проверьте, что ключ скопирован полностью (без пробелов)
3. Убедитесь, что проект активен в Supabase Dashboard

### Ошибка при выполнении SQL схемы

**Решение:**
1. Проверьте, что копируете весь файл `supabase/schema.sql`
2. Выполняйте SQL по частям, если есть ошибки
3. Проверьте логи ошибок в SQL Editor

### Ошибка: "Row Level Security policy violation"

**Решение:**
1. Убедитесь, что SQL схема выполнена полностью
2. Проверьте, что RLS политики созданы (они в конце `schema.sql`)
3. Убедитесь, что пользователь авторизован

---

## 📚 Дополнительные ресурсы

- [Документация Supabase](https://supabase.com/docs)
- [Supabase Auth Guide](https://supabase.com/docs/guides/auth)
- [Row Level Security](https://supabase.com/docs/guides/auth/row-level-security)

---

## 🎉 Готово!

После выполнения всех шагов Supabase будет полностью подключен к вашему проекту VEOX!

**Следующие шаги:**
- Настройте Stripe (см. `SETUP.md`)
- Настройте Email уведомления (см. `EMAIL_SETUP.md`)
- Начните тестировать функциональность

